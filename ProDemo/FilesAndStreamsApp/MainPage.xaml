<Page
    x:Class="FilesAndStreamsApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FilesAndStreamsApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar IsOpen="True">
            <AppBarButton Icon="OpenFile" Label="打开" Click="{x:Bind OnOpen}"></AppBarButton>
            <AppBarButton Icon="Save" Label="保存" Click="{x:Bind OnSave}"></AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox x:Name="myText" AcceptsReturn="True"></TextBox>
    </Grid>
</Page>
